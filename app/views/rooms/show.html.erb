<h1>#<%= @room.name %></h1>

<div id="messages" data-room="<%= @room.name %>" data-room-id="<%= @room.id %>">
  <%= render @room.messages %>
</div>

<form>
  <label>Say Something:</label><br />
  <input type="text" data-behavior="room_speaker">
</form>

<p><%= link_to 'Back to home', root_path %></p>
